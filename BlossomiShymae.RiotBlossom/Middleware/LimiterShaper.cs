namespace BlossomiShymae.RiotBlossom.Middleware
{
    /// <summary>
    /// An enum that represents the shaping mechanism for <see cref="AlgorithmicLimiter"/>.
    /// </summary>
    public enum LimiterShaper
    {
        Burst
    }
}
